plugins {
    id 'java-library'
    id 'java-conventions'
}

group 'io.github.gabrmsouza.subscription.application'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}